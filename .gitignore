node_modules/
.next/
.env*.local
website-content.md
website-spec.md

# Personal documents — the résumé is served from Vercel Blob, never committed.
# This repo is public; keep the PDF out of it.
private/
# OS + build artifacts
.DS_Store
tsconfig.tsbuildinfo

# Working notes — useful locally, not repo content.
docs/
