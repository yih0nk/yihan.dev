User-agent: *
Allow: /

# The résumé is password-gated; there is nothing behind it worth indexing.
Disallow: /resume

# AI agents: fetch /llms.txt — it is the index and the full plain-text
# rendering of this site in one file, with no CSS or animation to parse.
